# Desarrollo local (front en :5173, API en :3001)
VITE_API_URL=http://localhost:3001/api

# En Render no hace falta esta variable: el build de producción usa /api
# (mismo dominio para el catálogo y la API).
