# Base de datos SQLite (no requiere Docker ni instalación).
# Local: se crea como server/data.db. En Render: /data/data.db (disco persistente).
DATABASE_FILE="./data.db"

# Carpeta de fotos (perfumes y logo). En Render: /data/uploads
# UPLOAD_DIR="./uploads"

JWT_SECRET="cambia-esta-clave-en-produccion-por-una-larga-y-aleatoria"
JWT_EXPIRES_IN="7d"
PORT=3001

# Credenciales del primer usuario admin (solo se usan la primera vez que se crea la base)
ADMIN_EMAIL="admin@maisonambar.com"
ADMIN_PASSWORD="admin1234"
